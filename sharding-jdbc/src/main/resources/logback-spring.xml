<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <contextName>${APP_NAME}</contextName>
    <!--<contextName>${LOG_ROOT_PATH}</contextName>-->
    <springProperty name="APP_NAME" scope="context" source="spring.application.name"/>
    <springProperty name="LOG_ROOT_PATH" scope="context" source="geely.ros.logPath" defaultValue=""/>
    <springProperty name="LOG_FILE" scope="context" source="logging.file" defaultValue="${LOG_ROOT_PATH}/${APP_NAME}"/>

    <conversionRule conversionWord="wEx" converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter" />
    <property name="LOG_PATTERN" value="[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%p] - {&quot;thread&quot;:&quot;%t&quot;,&quot;class&quot;:&quot;%c&quot;,&quot;line&quot;:&quot;%L&quot;,&quot;message&quot;:&quot;%m${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}&quot;}%n" />

    <!-- 控制台日志 -->
    <appender name="StdoutAppender" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    <!-- 按照每天生成常规日志文件 -->
    <appender name="FileAppender" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_FILE}/${APP_NAME}.info.log</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="ErrorFileAppender" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_FILE}/${APP_NAME}.error.log</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

	<logger name="org.springframework" level="INFO"></logger>
	<logger name="org.apache" level="INFO"></logger>
	<logger name="com.zaxxer.hikari" level="INFO"></logger>

    <root level="INFO">
        <appender-ref ref="StdoutAppender"/>
        <appender-ref ref="FileAppender"/>
        <appender-ref ref="ErrorFileAppender"/>
    </root>
</configuration>
